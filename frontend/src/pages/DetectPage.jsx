// ========================================
// Detect Page — Placeholder
// ========================================

const DetectPage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-3xl font-bold text-text">Detect Page</h1>
    </div>
  );
};

export default DetectPage;
